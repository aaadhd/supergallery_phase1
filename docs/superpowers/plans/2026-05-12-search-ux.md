# 검색 UI/UX 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 검색 결과를 전시 카드 플랫 리스트 + 필터 탭(전체/작가명/그룹명/전시명/작품명)으로 개편한다.

**Architecture:** `searchRank.ts`에서 4개 필드별 매칭 배열을 반환하고, `Search.tsx`에서 필터 탭 상태에 따라 해당 배열을 렌더링한다. 카드는 기존 인라인 전시 카드 구조 유지.

**Tech Stack:** React, TypeScript, Tailwind CSS, `useI18n`

---

### Task 1: searchRank.ts — 필드별 분리 반환으로 교체

**Files:**
- Modify: `src/app/utils/searchRank.ts`

- [ ] **Step 1: 파일 전체를 아래 코드로 교체**

```ts
import type { Work } from '../data';

export type SearchResults = {
  /** 4개 필드 union, 중복 제거, uploadedAt 최신순 */
  all: Work[];
  byArtist: Work[];
  byGroup: Work[];
  byExhibition: Work[];
  byPiece: Work[];
};

export function searchWorks(pool: Work[], query: string): SearchResults {
  const lower = query.trim().toLowerCase();
  const empty: SearchResults = { all: [], byArtist: [], byGroup: [], byExhibition: [], byPiece: [] };
  if (!lower) return empty;

  const byArtist = pool.filter((w) => w.artist?.name?.toLowerCase().includes(lower));
  const byGroup = pool.filter((w) => w.groupName?.toLowerCase().includes(lower));
  const byExhibition = pool.filter((w) =>
    (w.exhibitionName?.toLowerCase().includes(lower)) ||
    (w.title?.toLowerCase().includes(lower))
  );
  const byPiece = pool.filter((w) =>
    w.imagePieceTitles?.some((t) => t?.toLowerCase().includes(lower))
  );

  const seen = new Set<string>();
  const all = [...byArtist, ...byGroup, ...byExhibition, ...byPiece]
    .filter((w) => { if (seen.has(w.id)) return false; seen.add(w.id); return true; })
    .sort((a, b) => (b.uploadedAt ?? '').localeCompare(a.uploadedAt ?? ''));

  return { all, byArtist, byGroup, byExhibition, byPiece };
}
```

- [ ] **Step 2: 타입 체크**

```bash
cd "/Users/im_1688/Documents/vibe/Supergallery 복사본" && npx tsc --noEmit 2>&1
```

기대: 오류 없음

- [ ] **Step 3: 커밋**

```bash
git add src/app/utils/searchRank.ts
git commit -m "refactor(search): searchRank를 필드별 분리 반환으로 교체"
```

---

### Task 2: messages.ts — 검색 필터 탭 i18n 키 추가

**Files:**
- Modify: `src/app/i18n/messages.ts`

- [ ] **Step 1: KO 사전에서 `'search.worksHeading'` 바로 아래에 추가**

```ts
  'search.filterAll': '전체',
  'search.filterArtist': '작가명',
  'search.filterGroup': '그룹명',
  'search.filterExhibition': '전시명',
  'search.filterPiece': '작품명',
```

- [ ] **Step 2: EN 사전에서 `'search.worksHeading'` 바로 아래에 추가**

```ts
  'search.filterAll': 'All',
  'search.filterArtist': 'Artist name',
  'search.filterGroup': 'Group name',
  'search.filterExhibition': 'Exhibition name',
  'search.filterPiece': 'Work title',
```

- [ ] **Step 3: 타입 체크**

```bash
cd "/Users/im_1688/Documents/vibe/Supergallery 복사본" && npx tsc --noEmit 2>&1
```

기대: 오류 없음

- [ ] **Step 4: 커밋**

```bash
git add src/app/i18n/messages.ts
git commit -m "feat(i18n): 검색 필터 탭 레이블 키 추가"
```

---

### Task 3: Search.tsx — results 계산 교체 + 필터 탭 상태 추가

**Files:**
- Modify: `src/app/pages/Search.tsx`

- [ ] **Step 1: import 교체 — `searchRank`에서 `searchWorks`와 `SearchResults` 임포트**

파일 상단에서 기존 `rankWorksBySearchQuery` 관련 import는 이미 제거됐으므로, 새 import를 추가:

```ts
import { searchWorks, type SearchResults } from '../utils/searchRank';
```

- [ ] **Step 2: `filterTab` 상태 추가 (기존 state 선언들 바로 아래)**

```ts
type FilterTab = 'all' | 'artist' | 'group' | 'exhibition' | 'piece';
const [filterTab, setFilterTab] = useState<FilterTab>('all');
```

- [ ] **Step 3: `results` useMemo 교체**

기존 `results` useMemo 전체를 아래로 교체:

```ts
const results = useMemo((): SearchResults => {
  const empty: SearchResults = { all: [], byArtist: [], byGroup: [], byExhibition: [], byPiece: [] };
  if (!searchTerm) return empty;
  const hiddenWorks = getHiddenWorkIdsForReporter();
  const pool = works.filter((w: Work) => !hiddenWorks.has(w.id) && isWorkVisibleOnPublicFeed(w));
  return searchWorks(pool, searchTerm);
}, [searchTerm, works, auth.isLoggedIn(), profileSig]);
```

- [ ] **Step 4: `hasResults`와 `totalCount` 교체**

```ts
const hasResults = results.all.length > 0;
const displayedWorks = useMemo(() => {
  switch (filterTab) {
    case 'artist': return results.byArtist;
    case 'group': return results.byGroup;
    case 'exhibition': return results.byExhibition;
    case 'piece': return results.byPiece;
    default: return results.all;
  }
}, [filterTab, results]);
```

- [ ] **Step 5: 검색어 변경 시 탭 초기화 — `doSearch` 함수에 `setFilterTab('all')` 추가**

```ts
const doSearch = (term: string) => {
  const trimmed = term.trim();
  if (!trimmed) return;
  addRecent(trimmed);
  setSearchParams({ q: trimmed });
  setQuery(trimmed);
  setFilterTab('all');
};
```

- [ ] **Step 6: 타입 체크**

```bash
cd "/Users/im_1688/Documents/vibe/Supergallery 복사본" && npx tsc --noEmit 2>&1
```

기대: 오류 없음

- [ ] **Step 7: 커밋**

```bash
git add src/app/pages/Search.tsx src/app/utils/searchRank.ts
git commit -m "refactor(search): results를 SearchResults 타입으로 교체, filterTab 상태 추가"
```

---

### Task 4: Search.tsx — JSX 결과 섹션 교체 (필터 탭 + 플랫 그리드)

**Files:**
- Modify: `src/app/pages/Search.tsx`

- [ ] **Step 1: 기존 검색 결과 섹션 전체를 아래로 교체**

`{/* Search results */}` 블록 전체(searchTerm && ... 내부)를 교체:

```tsx
{searchTerm && (
  <div className="space-y-5 sm:space-y-6">
    {/* 결과 건수 */}
    <p className="text-sm text-muted-foreground">
      {t('search.resultsLine')
        .replace('{q}', searchTerm)
        .replace('{n}', String(results.all.length))}
    </p>

    {/* 필터 탭 */}
    {hasResults && (
      <div className="flex gap-2 flex-wrap">
        {(
          [
            { key: 'all', label: t('search.filterAll'), count: results.all.length },
            { key: 'artist', label: t('search.filterArtist'), count: results.byArtist.length },
            { key: 'group', label: t('search.filterGroup'), count: results.byGroup.length },
            { key: 'exhibition', label: t('search.filterExhibition'), count: results.byExhibition.length },
            { key: 'piece', label: t('search.filterPiece'), count: results.byPiece.length },
          ] as const
        ).map(({ key, label, count }) => (
          <button
            key={key}
            type="button"
            onClick={() => setFilterTab(key as FilterTab)}
            className={`min-h-[44px] px-4 py-2 rounded-full text-sm font-medium transition-colors ${
              filterTab === key
                ? 'bg-foreground text-white'
                : count === 0
                ? 'bg-muted/50 text-muted-foreground/50 cursor-default'
                : 'bg-muted text-muted-foreground lg:hover:bg-muted/70'
            }`}
            disabled={count === 0 && key !== 'all'}
          >
            {label} {count}
          </button>
        ))}
      </div>
    )}

    {/* 결과 그리드 */}
    {displayedWorks.length > 0 && (
      <div className="grid grid-cols-2 sm:grid-cols-3 gap-4">
        {displayedWorks.map((work) => (
          <button
            type="button"
            key={work.id}
            onClick={() => navigate(`/exhibitions/${work.id}`)}
            className="group text-left"
          >
            <div className="aspect-square bg-white rounded-xl overflow-hidden border border-border mb-2">
              <ImageWithFallback
                src={imageUrls[getThumbCover(work)] || getThumbCover(work)}
                alt={displayProminentHeadline(work, t('work.untitled'))}
                className="w-full h-full object-contain hover-scale"
              />
            </div>
            <h3 className="text-sm font-medium text-foreground truncate lg:group-hover:text-primary transition-colors">
              {displayProminentHeadline(work, t('work.untitled'))}
            </h3>
            <p className="text-xs text-muted-foreground truncate">{work.artist.name}</p>
          </button>
        ))}
      </div>
    )}

    {/* 필터 탭 결과 없음 */}
    {hasResults && displayedWorks.length === 0 && (
      <div className="text-center py-10">
        <p className="text-sm text-muted-foreground">
          {t('search.noResults').replace('{query}', `${searchTerm} (${
            filterTab === 'artist' ? t('search.filterArtist') :
            filterTab === 'group' ? t('search.filterGroup') :
            filterTab === 'exhibition' ? t('search.filterExhibition') :
            t('search.filterPiece')
          })`)}
        </p>
      </div>
    )}

    {/* 전체 결과 없음 */}
    {!hasResults && (
      <div className="text-center py-12 sm:py-20">
        <SearchIcon className="h-12 w-12 text-muted-foreground/20 mx-auto mb-3" />
        <h3 className="text-sm sm:text-base font-semibold text-foreground mb-2">
          {t('search.noResults').replace('{query}', searchTerm)}
        </h3>
        <p className="text-sm text-muted-foreground mb-6">{t('search.noResultsHint')}</p>
        {recentSearches.length > 0 && (
          <div className="mb-6">
            <p className="text-xs text-muted-foreground mb-2">{t('search.tryRecent')}</p>
            <div className="flex flex-wrap items-center justify-center gap-2">
              {recentSearches.slice(0, 5).map((term) => (
                <button
                  key={term}
                  type="button"
                  onClick={() => { setQuery(term); setSearchParams({ q: term }); }}
                  className="min-h-[44px] rounded-full border border-border px-4 text-sm text-foreground lg:hover:bg-muted/50"
                >
                  {term}
                </button>
              ))}
            </div>
          </div>
        )}
        <button
          type="button"
          onClick={() => navigate('/')}
          className="min-h-[44px] rounded-full bg-primary text-white px-6 text-sm font-medium lg:hover:opacity-90"
        >
          {t('search.goBrowse')}
        </button>
      </div>
    )}
  </div>
)}
```

- [ ] **Step 2: 사용하지 않는 import 정리 확인**

`Avatar`, `AvatarFallback`, `AvatarImage`가 여전히 쓰이는지 확인. 작가 섹션이 제거됐으므로 불필요하면 import에서 제거:

```bash
grep -n "Avatar\|AvatarFallback\|AvatarImage" "/Users/im_1688/Documents/vibe/Supergallery 복사본/src/app/pages/Search.tsx"
```

쓰이지 않으면 import 한 줄 삭제.

- [ ] **Step 3: 타입 체크**

```bash
cd "/Users/im_1688/Documents/vibe/Supergallery 복사본" && npx tsc --noEmit 2>&1
```

기대: 오류 없음

- [ ] **Step 4: 커밋**

```bash
git add src/app/pages/Search.tsx
git commit -m "feat(search): 필터 탭 + 전시 카드 플랫 리스트로 결과 화면 개편"
```

---

### Task 5: 브라우저 확인

- [ ] **Step 1: 개발 서버 확인 (이미 실행 중이면 스킵)**

```bash
# 이미 실행 중: http://localhost:5179
```

- [ ] **Step 2: "봄" 검색 — 전체 탭에서 최신순 카드 리스트 확인**

- [ ] **Step 3: 각 필터 탭 클릭 — 해당 필드 매칭 결과만 표시되는지 확인**

- [ ] **Step 4: 결과 없는 탭은 비활성(흐리게) 표시되는지 확인**

- [ ] **Step 5: 없는 키워드 검색 — 빈 결과 안내 화면 확인**
